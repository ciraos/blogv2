#! env.example

# ─────────────────────────────────────────────────────────────
# 站点地址（origin，不带路径）。用于 SEO、站点地图 /sitemap 等。
NEXT_PUBLIC_SITE_URL=https://blog.example.com

# 远端 API 基址（带 /api 后缀）。供 src/lib/api.ts 等服务端直连后端使用。
# 后端与站点同源时，通常就是 <SITE_URL>/api。
NEXT_PUBLIC_API_URL=https://blog.example.com/api

# ─────────────────────────────────────────────────────────────
# 可选：next.config.ts 的 rewrites() 代理后端时使用的后端基址。
#
#   优先级：BACKEND_URL(dev) / API_URL(生产)   >  NEXT_PUBLIC_SITE_URL  >  通用兜底
#            （显式指定）                       （同源自动回退）            localhost:3126 / anheyu:3126
#
# 后端与站点同源时【无需配置】——只要设了 NEXT_PUBLIC_SITE_URL，rewrites() 会自动回退取它的值，
# 不必重复写后端地址。
# 仅当后端与站点【不同源】（例如后端单独部署在别的域名/端口）时才需要显式指定：
#   BACKEND_URL=http://localhost:3126     # dev 环境的后端地址
#   API_URL=https://api.example.com        # 生产（非 Docker）环境的后端地址
#
# 注意：这两个变量应写「不带 /api 后缀」的 host，rewrites() 会自行拼 /api/:path*、/f/:path*。
